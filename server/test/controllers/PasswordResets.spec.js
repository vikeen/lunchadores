"use strict";

var should = require('should'),
    _ = require('lodash'),
    models = require('../../app/models'),
    passwordResetController = require('./../../app/controllers/PasswordResets.js');

describe('PasswordResets Controller', function () {
    before(function (done) {
        done();
    });

    beforeEach(function (done) {
        done();
    });

    it('should serve as a dummy test', function (done) {
        done();
    });
});
