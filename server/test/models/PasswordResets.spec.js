'use strict';

var should = require('should'),
    models = require('../../app/models');

describe('PasswordResets Model', function () {
    before(function (done) {
        done();
    });

    beforeEach(function (done) {
        done();
    });

    it("should pass as dummy test", function (done) {
        done();
    });
});
